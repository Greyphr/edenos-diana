"""Block 2: speaker identity.

Energy-based utterance segmentation (vad.py), speaker embeddings
(embeddings.py), voiceprint persistence (voiceprint_store.py), speaker
recognition (recognition.py), and a standalone enrollment CLI (enroll.py).
"""