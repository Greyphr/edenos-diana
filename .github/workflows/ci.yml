name: CI

on:
  push:
  pull_request:

jobs:
  check:
    name: pyright + pytest
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.14"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          # openwakeword: removed from requirements.txt (see its comment).
          # --no-deps skips the unresolvable Linux-only tflite-runtime pin.
          pip install "openwakeword>=0.6.0" --no-deps
          pip install -r requirements.txt
          pip install -r requirements-dev.txt

      - name: Type check (pyright, 0 errors required)
        run: pyright

      - name: Run tests (pytest, all passing required)
        run: pytest